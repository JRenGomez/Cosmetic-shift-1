using UnityEngine;

public class UsualView : View
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public UsualView()
    {
        this.ViewType = ViewType.UsualView;
    }
    public override void ApplyView()
    {
    }
}
